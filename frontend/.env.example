# Entra ID SSO (per-developer / per-environment — real values never committed)
VITE_ENTRA_TENANT_ID=<entra-tenant-id>
VITE_ENTRA_CLIENT_ID=<spa-app-registration-client-id>
VITE_ENTRA_API_SCOPE=api://<api-app-id>/access_as_user

# Base URL of the Yvy API (includes the version segment)
VITE_API_URL=http://localhost:5224/api/v1
